# next-minimal

The smallest working Next.js 15 + `@ultrachess/react` example.

```bash
bun install
bun -F @ultrachess/example-next-minimal dev    # http://localhost:3001
```

Scaffolding until M2; the real board lands with `useChessGame` + `<Chessboard/>`.
