#!/usr/bin/env node
/**
 * Benchmark runner entry.
 *
 * Scaffolding placeholder. In M2+, this script will:
 * 1. Boot a headless browser (via Playwright) pointing at the built bundle.
 * 2. Execute each scenario (move, undo, drag, arrow-draw) under React Profiler.
 * 3. Write JSON to `bench-results/<scenario>.json`.
 * 4. Compare against `bench-results/baseline.json` and fail on >10% regression.
 */
console.log("[bench] scaffolding — real runs land in M2");
process.exit(0);
