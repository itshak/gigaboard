/**
 * gigaboard/canvas — opt-in full-canvas renderer.
 *
 * For stress use cases: very large boards (fairy chess), many simultaneous arrows,
 * or low-end devices where per-square DOM nodes become a cost. Drops some DOM
 * superpowers (ARIA grid, CSS theming of individual squares, `renderSquare` escape
 * hatches). The default DOM renderer is recommended for almost all applications.
 *
 * @remarks
 * Lands in M8 (post-1.0). This is a scaffolding placeholder.
 */

export const CANVAS_RENDERER_VERSION = "0.0.0";
