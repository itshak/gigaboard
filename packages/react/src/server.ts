/**
 * @ultrachess/react/server — server-only entry point.
 *
 * Renders a static board from a FEN string with zero client JS. Safe to use in a
 * Next.js React Server Component without a `"use client"` boundary.
 *
 * @remarks
 * Full implementation lands in M5. This is a scaffolding placeholder.
 */

export const SERVER_PACKAGE_VERSION = "0.0.0";
