/**
 * @ultrachess/pieces — SVG piece sets.
 *
 * Tree-shakeable: import a specific set from its sub-path
 * (e.g. `@ultrachess/pieces/cburnett`) rather than from this barrel, which
 * intentionally does not re-export every set to keep bundles small.
 *
 * @remarks
 * Individual sets land in M6. This file carries shared types only.
 */

export const PACKAGE_VERSION = "0.0.0";
